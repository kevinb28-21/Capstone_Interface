Local Testing Guide

This guide shows you how to run and test the image processing algorithm locally.

Quick Start

Option 1: Automated Script (Recommended)

Start all services
./run-local.sh

Stop all services
./stop-local.sh

Option 2: Manual Commands

See "Manual Setup" section below.

Prerequisites
  PostgreSQL must be running:
      brew services start postgresql@14
  Database credentials must be set:
      export DBPASSWORD="<yourpostgrespassword>"
   # Check connection
   PGPASSWORD="$DBPASSWORD" psql -U droneuser -d droneanalytics -c "SELECT 1;"
   
Starting Services Manually

Terminal 1: Python Background Worker

cd /Users/kevinbhatt/Desktop/Projects/CapstoneInterface/pythonprocessing
source venv/bin/activate
python3 backgroundworker.py

What it does:
  - Polls database for new images every 10 seconds
  - Processes images: calculates NDVI, SAVI, GNDVI
  - Saves analysis results to database
  - Logs to console and /tmp/worker.log

Watch logs:
tail -f /tmp/worker.log

Terminal 2: Node.js Backend

cd /Users/kevinbhatt/Desktop/Projects/CapstoneInterface/server
npm run dev

What it does:
  - Runs Express API on port 5050
  - Handles image uploads
  - Serves API endpoints
  - Logs to console

Test API:
curl http://localhost:5050/api/health

Terminal 3: React Frontend

cd /Users/kevinbhatt/Desktop/Projects/CapstoneInterface/client
npm run dev

What it does:
  - Runs Vite dev server on port 5182
  - Serves React application
  - Hot reload on code changes

Open in browser:
http://localhost:5182

Testing Image Processing
  Upload an Image
  Open http://localhost:5182
  Navigate to "Upload" or "Analytics" page
  Click "Upload Image" or drag & drop
  Select an image file (JPG, PNG)
  Monitor Processing

In the UI:
  - Status will change: uploaded → processing → completed
  - Processing time: ~10-15 seconds (including poll wait)

In Terminal (Worker logs):
tail -f /tmp/worker.log

You should see:
INFO - Found 1 pending image(s) to process
INFO - Processing image <id>: <filename>
INFO - Analyzing onion crop image: <path>
INFO - ✓ Successfully processed image <id>
  Check Results

In the UI:
  - Go to "Analytics" page
  - View NDVI, SAVI, GNDVI values
  - See health status and summary

In Database:
PGPASSWORD="$DBPASSWORD" psql -U droneuser -d droneanalytics -c "
SELECT 
    filename, 
    processingstatus,
    uploadedat,
    processedat
FROM images 
ORDER BY uploadedat DESC 
LIMIT 5;
"

View Analysis Results:
PGPASSWORD="$DBPASSWORD" psql -U droneuser -d droneanalytics -c "
SELECT 
    i.filename,
    a.ndvimean,
    a.savimean,
    a.gndvimean,
    a.healthstatus,
    a.summary
FROM images i
JOIN analyses a ON i.id = a.imageid
ORDER BY i.uploadedat DESC
LIMIT 5;
"

Troubleshooting

Worker Not Processing Images

Check worker is running:
ps aux | grep backgroundworker.py

Check database connection:
cd pythonprocessing
source venv/bin/activate
python3 -c "from dbutils import testconnection; print('OK' if testconnection() else 'FAILED')"

Check for pending images:
PGPASSWORD="$DBPASSWORD" psql -U droneuser -d droneanalytics -c "
SELECT COUNT() FROM images WHERE processingstatus = 'uploaded';
"

Images Not Found Error

Check file paths:
ls -la server/uploads/

Fix database paths:
PGPASSWORD="$DBPASSWORD" psql -U droneuser -d droneanalytics -c "
UPDATE images 
SET filepath = '/Users/kevinbhatt/Desktop/Projects/CapstoneInterface/server/uploads/' || filename,
    s3stored = false 
WHERE filepath IS NULL;
"

Backend Not Responding

Check if running:
curl http://localhost:5050/api/health

Check logs:
tail -f /tmp/server.log

Restart:
cd server
npm run dev

Frontend Not Loading

Check if running:
curl http://localhost:5182

Check logs:
tail -f /tmp/frontend.log

Clear cache and restart:
cd client
rm -rf nodemodules/.vite
npm run dev

Performance Testing

Test Processing Speed

Upload multiple images:
Upload 5 images quickly
Monitor processing time in logs
tail -f /tmp/worker.log | grep "Successfully processed"

Expected times:
  - Small images (< 1MB): ~0.1-0.2 seconds analysis
  - Medium images (1-5MB): ~0.5-2 seconds analysis
  - Large images (> 5MB): ~2-5 seconds analysis
  - Total time: +10 seconds (poll wait)

Test Concurrent Processing

The worker processes images in batches (default: 5 images per batch).

Monitor batch processing:
tail -f /tmp/worker.log | grep -E "Found|Processed.batch"

Useful Commands

View All Logs
tail -f /tmp/worker.log /tmp/server.log /tmp/frontend.log

Check Service Status
Worker
ps aux | grep backgroundworker

Backend
ps aux | grep "node.*server"

Frontend
ps aux | grep vite

Reset Failed Images
PGPASSWORD="$DBPASSWORD" psql -U droneuser -d droneanalytics -c "
UPDATE images 
SET processingstatus = 'uploaded' 
WHERE processingstatus = 'failed';
"

Clear All Images (Testing)
PGPASSWORD="$DBPASSWORD" psql -U droneuser -d drone_analytics -c "
DELETE FROM analyses;
DELETE FROM images;
"

Next Steps
  ✅ Test with different image sizes
  ✅ Test with multiple images
  ✅ Verify NDVI/SAVI/GNDVI calculations
  ✅ Check health status classifications
  ✅ Test error handling (invalid images, etc.)