# Node.js Backend Environment Variables
# Copy this to server/.env and fill in your values

PORT=5050
NODE_ENV=production
# CORS: Comma-separated list of allowed origins (e.g., "https://cropview.netlify.app,http://localhost:5173")
ORIGIN=https://cropview.netlify.app,http://localhost:5173

# AWS S3 Configuration
AWS_ACCESS_KEY_ID=your-aws-access-key-here
AWS_SECRET_ACCESS_KEY=your-aws-secret-key-here
AWS_REGION=us-east-2
S3_BUCKET_NAME=your-s3-bucket-name-here

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=drone_analytics
DB_USER=drone_user
DB_PASSWORD=your-database-password-here

